<UserControl x:Class="VorTech.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <!-- Syntaxe correcte : property elements -->
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- En-tête -->
    <TextBlock Grid.Row="0"
               Text="Dashboard"
               FontSize="{StaticResource FontSizeLarge}"
               FontWeight="SemiBold"
               Margin="0,0,0,8"/>

    <!-- Contenu provisoire : compile sans VM -->
    <Border Grid.Row="1"
            Background="{StaticResource Panel}"
            BorderBrush="{StaticResource Border}"
            BorderThickness="1"
            Padding="12">
      <TextBlock Text="(À venir) KPIs, derniers devis/factures, etc."
                 Foreground="{StaticResource Subtle}"/>
    </Border>
  </Grid>
</UserControl>
