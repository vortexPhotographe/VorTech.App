<UserControl x:Class="VorTech.App.Views.DevisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="860">
    <UserControl.Resources>
        <!-- TextBox sombre (pour les champs de saisie) -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#232323"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="Padding" Value="6,3"/>
        </Style>

        <!-- Items de ComboBox (ligne dans le menu déroulant) -->
        <Style x:Key="DarkComboItem" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Padding"    Value="8,4"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#383838"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#404040"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox sombre (non éditable, popup sombre) -->
        <Style x:Key="DarkCombo" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Foreground" Value="#D0D0D0"/>
            <Setter Property="Background" Value="#232323"/>
            <Setter Property="BorderBrush" Value="#3A3A3A"/>
            <Setter Property="Padding" Value="6,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>

                            <!-- zone sélection -->
                            <Border x:Name="Border" Grid.ColumnSpan="2"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1" CornerRadius="2"/>
                            <ContentPresenter x:Name="ContentSite"
                                      Margin="6,2,20,2"
                                      VerticalAlignment="Center"
                                      Content="{TemplateBinding SelectionBoxItem}"
                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>

                            <!-- bouton flèche -->
                            <ToggleButton Grid.Column="1"
                  x:Name="DropDownToggle"
                  Focusable="False"
                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}">
                                <Path HorizontalAlignment="Center" VerticalAlignment="Center"
            Data="M 0 0 L 4 4 L 8 0 Z" Fill="#D0D0D0"/>
                            </ToggleButton>

                            <!-- popup -->
                            <Popup x:Name="PART_Popup"
           Placement="Bottom"
           IsOpen="{TemplateBinding IsDropDownOpen}"
           AllowsTransparency="True" Focusable="False"
           PopupAnimation="Slide">
                                <Border Background="#2A2A2A" BorderBrush="#3A3A3A" BorderThickness="1">
                                    <ScrollViewer CanContentScroll="True">
                                        <StackPanel IsItemsHost="True"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- états -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4A4A4A"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#888"/>
                                <Setter TargetName="Border" Property="Background" Value="#1C1C1C"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2C2C2C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <!-- style appliqué aux items -->
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Toolbar -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0" Margin="8" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Devis" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock Text=" " Width="8"/>
                <TextBlock Text="{Binding Numero}" FontSize="16" Opacity="0.8" VerticalAlignment="Center"/>
                <TextBlock Text=" • " Margin="8,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Etat}" VerticalAlignment="Center"/>
                <TextBlock Text=" • " Margin="8,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" Margin="200,0">
                <Button Content="Enregistrer" Click="Save_Click" Margin="4,0"/>
                <Button x:Name="BtnSendMail"
                    Content="Envoyer par e-mail"
                    Click="BtnSendMail_Click"
                    Margin="4,0"
                    ToolTip="Envoie le PDF du devis par e-mail au client">
                    <Button.Style>
                        <!-- IMPORTANT : BasedOn pour hériter du style global des boutons -->
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <!-- Cache si Numero est null -->
                                <DataTrigger Binding="{Binding Numero}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <!-- Cache si Numero est vide -->
                                <DataTrigger Binding="{Binding Numero}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="BtnRegenPdf"
                        Content="Générer PDF"
                        Padding="12,6"
                        Margin="4,0"
                        Click="BtnRegenPdf_Click">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <!-- Par défaut activé -->
                            <Setter Property="IsEnabled" Value="True"/>
                            <!-- Désactiver si Numero est null/vidé -->
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Numero}" Value="{x:Null}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Numero}" Value="">
                                    <Setter Property="IsEnabled" Value="False"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button x:Name="BtnEmit" Content="Émettre (numéro)" Click="BtnEmit_Click" Margin="4,0"/>
                <Button Content="Supprimer" Click="Delete_Click" Margin="4,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Corps en 3 colonnes : Liste devis | Fiche | Annexes -->
        <Grid Grid.Row="1" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <!-- Liste devis -->
                <ColumnDefinition Width="*"/>
                <!-- Fiche devis -->
                <ColumnDefinition Width="420"/>
                <!-- Annexes + compte bancaire -->
            </Grid.ColumnDefinitions>

            <!-- Colonne gauche : Liste des devis + recherche -->
            <DockPanel Grid.Column="0" Margin="0,0,12,0">
                <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <TextBlock Text="Recherche" FontWeight="Bold" Margin="0,0,0,4"/>
                    <TextBox x:Name="SearchBox" Width="220"
                           ToolTip="Numéro de devis, nom/prénom/société"
                           TextChanged="OnSearchTextChanged"/>
                </StackPanel>

                <!-- LISTE DES DEVIS -->
                <GroupBox Header="Devis" DockPanel.Dock="Bottom">
                    <ListBox x:Name="DevisList"
         SelectionChanged="OnListSelectionChanged"
         Background="#252525" BorderBrush="#3A3A3A" Foreground="#EFEFEF"
         ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="6"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="10" BorderThickness="1" BorderBrush="#404040" CornerRadius="3" Background="#2B2B2B">
                                    <StackPanel>
                                        <!-- L1 : Numéro (ou Brouillon) -->
                                        <TextBlock FontWeight="SemiBold">
                                            <TextBlock.Text>
                                                <Binding Path="Numero" TargetNullValue="Brouillon">
                                                    <Binding.FallbackValue>Brouillon</Binding.FallbackValue>
                                                </Binding>
                                            </TextBlock.Text>
                                        </TextBlock>

                                        <!-- L2 : Société si présente -->
                                        <TextBlock Text="{Binding ClientSociete}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ClientSociete}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ClientSociete}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- L2 bis : Nom Prénom si pas de société -->
                                        <TextBlock Text="{Binding ClientNomPrenom}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ClientSociete}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ClientSociete}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- L2 ter : Fallback legacy -> ClientNom -->
                                        <TextBlock Text="{Binding ClientNom}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding ClientSociete}" Value=""/>
                                                                <Condition Binding="{Binding ClientNomPrenom}" Value=""/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding ClientSociete}" Value="{x:Null}"/>
                                                                <Condition Binding="{Binding ClientNomPrenom}" Value="{x:Null}"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- L2 quad : rien du tout -> message -->
                                        <TextBlock Text="(Destinataire non renseigné)" Foreground="#B0B0B0" FontStyle="Italic">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding ClientSociete}" Value=""/>
                                                                <Condition Binding="{Binding ClientNomPrenom}" Value=""/>
                                                                <Condition Binding="{Binding ClientNom}" Value=""/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                        <MultiDataTrigger>
                                                            <MultiDataTrigger.Conditions>
                                                                <Condition Binding="{Binding ClientSociete}" Value="{x:Null}"/>
                                                                <Condition Binding="{Binding ClientNomPrenom}" Value="{x:Null}"/>
                                                                <Condition Binding="{Binding ClientNom}" Value="{x:Null}"/>
                                                            </MultiDataTrigger.Conditions>
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </MultiDataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>

                                        <!-- L3 : État -->
                                        <TextBlock Text="{Binding Etat}" FontStyle="Italic" Foreground="#B0B0B0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </GroupBox>
            </DockPanel>

            <!-- Colonne centrale : Fiche devis -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,0,12,0">
                    <!-- Destinataire -->
                    <GroupBox Header="Destinataire" Margin="0,0,0,12">
                        <!-- IMPORTANT : un seul enfant direct du GroupBox -->
                        <Grid Margin="8">
                            <!-- Ces deux blocs DOIVENT être directement sous <Grid> -->
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="160"/>
                                <!-- labels -->
                                <ColumnDefinition Width="*"/>
                                <!-- champs -->
                                <ColumnDefinition Width="160"/>
                                <!-- labels -->
                                <ColumnDefinition Width="*"/>
                                <!-- champs -->
                            </Grid.ColumnDefinitions>

                            <!-- Ligne 0 : Titre (champ libre) + Sélection client -->
                            <Label Grid.Row="0" Grid.Column="0" Content="Titre" VerticalAlignment="Center"/>
                            <Grid Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" x:Name="TxtClientQuick"
                                         ToolTip="Titre libre du devis (facultatif)"
                                         Margin="0,4,8,4"/>
                                <Button Grid.Column="1" Content="Sélectionner…" Padding="10,6"
                                        Click="SelectClient_Click"/>
                            </Grid>

                            <!-- Ligne 1 : Société / Nom Prénom -->
                            <Label Grid.Row="1" Grid.Column="0" Content="Société" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,4,8,4"
                                     Text="{Binding ClientSociete, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>
                            <Label Grid.Row="1" Grid.Column="2" Content="Nom Prénom" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="3" Margin="0,4,8,4"
                                     Text="{Binding ClientNomPrenom, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>

                            <!-- Ligne 2 : Adresse -->
                            <Label Grid.Row="2" Grid.Column="0" Content="Adresse" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3" Margin="0,4,8,4"
                                     Text="{Binding ClientAdresseL1, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>

                            <!-- Ligne 3 : CP / Ville -->
                            <Label Grid.Row="3" Grid.Column="0" Content="Code postal" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Margin="0,4,8,4" Width="120"
                                     Text="{Binding ClientCodePostal, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>
                            <Label Grid.Row="3" Grid.Column="2" Content="Ville" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Grid.Column="3" Margin="0,4,8,4"
                                     Text="{Binding ClientVille, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>

                            <!-- Ligne 4 : Email / Téléphone -->
                            <Label Grid.Row="4" Grid.Column="0" Content="Email" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Margin="0,4,8,4"
                                     Text="{Binding ClientEmail, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>
                            <Label Grid.Row="4" Grid.Column="2" Content="Téléphone" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="4" Grid.Column="3" Margin="0,4,8,4"
                                     Text="{Binding ClientTelephone, UpdateSourceTrigger=LostFocus}"
                                     LostFocus="ClientFields_LostFocus"/>
                        </Grid>
                    </GroupBox>

                    <!-- Lignes -->
                    <GroupBox Header="Lignes" Margin="0,0,0,12">
                        <DockPanel Margin="8">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
                                <Button Content="+ Ajouter"  Margin="0,0,8,0" Click="AddLine_Click"/>
                                <Button Content="Dupliquer"  Margin="0,0,8,0" Click="DuplicateLine_Click"/>
                                <Button Content="Supprimer"  Click="DeleteLine_Click"/>
                            </StackPanel>

                            <DataGrid x:Name="GridLines"
                                      ItemsSource="{Binding Lignes}" AutoGenerateColumns="False" 
                                      CanUserAddRows="False"
                                      CellEditEnding="CellEditEnded">
                                <DataGrid.RowStyle>
                                    <Style TargetType="DataGridRow">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsStockOk}" Value="False">
                                                <Setter Property="Foreground" Value="Red"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGrid.RowStyle>
                                <DataGrid.Columns>
                                    <!-- Image -->
                                    <DataGridTemplateColumn Header=" " Width="48">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ImagePath}" Stretch="UniformToFill" Width="40" Height="40"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <DataGridTextColumn Header="Désignation" Binding="{Binding Designation, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                                    <DataGridTextColumn Header="Qté" Binding="{Binding Qty, StringFormat={}{0:#0.##}, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                                    <DataGridTextColumn Header="PU (€)" Binding="{Binding PU, StringFormat={}{0:0.##}, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                                    <DataGridTextColumn Header="Montant (€)" IsReadOnly="True"
                                                        Binding="{Binding Montant, StringFormat={}{0:0.##}}" Width="120"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </DockPanel>
                    </GroupBox>

                    <!-- Remise + Total -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Remise globale (€)" VerticalAlignment="Center"/>
                            <TextBlock Text=" " Width="8"/>

                            <!-- ✅ Binding en markup étendu pour éviter les erreurs de parsing -->
                            <TextBox x:Name="TxtRemise" Width="140" Text="{Binding RemiseGlobale, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Total :" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text=" " Width="8"/>
                            <TextBlock>
                                <TextBlock.Text>
                                    <Binding Path="Total" StringFormat="{}{0:0.##} €"/>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Vertical">
                            <TextBlock x:Name="TxtCostMatiere" Text="Coût matières : 0 €" />
                            <TextBlock x:Name="TxtCharges"     Text="Charges estimées : 0 €" />
                            <TextBlock x:Name="TxtMarge"       Text="Marge brute : 0 €" FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <!-- Colonne droite : Annexes + compte bancaire -->
                <ScrollViewer Grid.Column="2"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          CanContentScroll="True"
                          Padding="0">
                    <StackPanel x:Name="RightPane" Orientation="Vertical">
                        <GroupBox Header="Textes du devis" Margin="0,0,0,12"
                                Background="#FF2B2B2B" BorderBrush="#FF404040" Foreground="#FFEFEFEF">
                            <StackPanel Margin="8" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Éditer le texte (haut)" Click="EditNoteHaut_Click" Padding="10,6" Margin="0,0,8,0"/>
                                <Button Content="Éditer le texte (bas)"  Click="EditNoteBas_Click"  Padding="10,6"/>
                            </StackPanel>
                        </GroupBox>
                        <!-- Options (0–3) -->
                        <GroupBox Header="Options (0–3)" Margin="0,8,0,8">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                </Grid.ColumnDefinitions>
                                <!-- Tableau des options -->
                                <DataGrid x:Name="GridOptions"
                                      Grid.Row="0"
                                      Grid.Column="0"
                                      Grid.ColumnSpan="2"
                                      AutoGenerateColumns="False"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      IsReadOnly="True"
                                      HeadersVisibility="Column"
                                      MaxHeight="220"
                                      Background="{DynamicResource Panel}"
                                      BorderBrush="{DynamicResource Border}"
                                      Foreground="{DynamicResource Fore}">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Libellé" Binding="{Binding Libelle}" Width="3*"/>
                                        <DataGridTextColumn Header="Prix (€)" Binding="{Binding Prix, StringFormat={}{0:0.00}}" Width="*" >
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextAlignment" Value="Right"/>
                                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                                <!-- Boutons -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="BtnAddOption"
                                        Content="Ajouter une option…" Margin="0,0,8,0"
                                        Click="BtnAddOption_Click"/>
                                    <Button x:Name="BtnDeleteOption"
                                        Content="Supprimer"
                                        Click="BtnDeleteOption_Click"/>
                                </StackPanel>
                            </Grid>
                        </GroupBox>
                        <!-- Annexes -->
                        <GroupBox Header="Annexes (attachées au devis)" Margin="0,0,0,12">
                            <StackPanel Margin="8">
                                <!-- Liste des annexes déjà attachées au devis -->
                                <ListBox x:Name="ListAnnexes"
                                     Height="220"
                                     Background="{DynamicResource Panel}"
                                     BorderBrush="{DynamicResource Border}"
                                     Foreground="{DynamicResource Fore}"
                                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <DockPanel LastChildFill="True">
                                                <TextBlock Text="{Binding Display}" VerticalAlignment="Center"/>
                                                <Button Content="Retirer"
                                                    DockPanel.Dock="Right"
                                                    Margin="8,0,0,0"
                                                    Padding="8,2"
                                                    Tag="{Binding Id}"
                                                    Click="RemoveAnnex_Click"/>
                                            </DockPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                                <!-- Actions -->
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                    <Button x:Name="BtnAddAnnexFromCatalog"
                                      Content="Ajouter depuis le catalogue…"
                                      Click="BtnAddAnnexFromCatalog_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Modélitée de reglement" Margin="0,12,0,0">
                            <StackPanel Margin="8">
                                <ComboBox x:Name="CmbPayment"
                                      Width="260"
                                      Style="{StaticResource DarkCombo}"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="Id"
                                      SelectedValue="{Binding PaymentTermsId, Mode=TwoWay}"
                                      SelectionChanged="CmbPayment_SelectionChanged"/>
                                <TextBlock Text="Choisissez les modélités de reglements."
                                    Foreground="#B0B0B0" Margin="0,6,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Compte bancaire (pied de page)" Margin="0,12,0,0">
                            <StackPanel Margin="8">
                                <ComboBox x:Name="CmbBank"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValuePath="Id"
                                      Style="{StaticResource DarkCombo}"
                                      SelectedValue="{Binding BankAccountId, Mode=TwoWay}"
                                      SelectionChanged="CmbBank_SelectionChanged"
                                      Width="260"/>
                                <TextBlock Text="Choisissez le compte qui sera affiché en pied de page du PDF."
                                        Foreground="#B0B0B0" Margin="0,6,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
