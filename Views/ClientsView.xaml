<UserControl x:Class="VorTech.App.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Clients" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>

    <DataGrid x:Name="GridClients"
              Grid.Row="1"
              AutoGenerateColumns="False"
              CanUserAddRows="True"
              CanUserDeleteRows="True"
              IsReadOnly="False"
              Margin="0,0,0,8">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Nom" Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Width="2*"/>
        <DataGridTextColumn Header="Adresse" Binding="{Binding Address}" Width="3*"/>
        <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="2*"/>
        <DataGridTextColumn Header="Téléphone" Binding="{Binding Phone}" Width="*"/>
        <DataGridTextColumn Header="SIRET" Binding="{Binding Siret}" Width="*"/>
        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="2*"/>
      </DataGrid.Columns>
    </DataGrid>

    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Enregistrer" Click="Save_Click"/>
      <Button Content="Supprimer"   Click="Delete_Click"/>
      <Button Content="Recharger"   Click="Reload_Click"/>
    </StackPanel>
  </Grid>
</UserControl>
