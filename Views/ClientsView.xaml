<UserControl x:Class="VorTech.App.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Colonne gauche : liste compacte -->
        <DockPanel Grid.Column="0">
            <TextBlock Text="Clients" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
            <ListBox ItemsSource="{Binding Clients}"
                     SelectedItem="{Binding SelectedClient, Mode=TwoWay}"
                     Margin="0,8,0,0">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <TextBlock Text="{Binding Nom}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Prenom}" />
                            <TextBlock Text="{Binding NomTeam}" FontStyle="Italic" Opacity="0.8"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>

        <!-- Separateur -->
        <Border Grid.Column="1" Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" Opacity="0.2"/>

        <!-- Colonne droite : fiche client -->
        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Barre d actions -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <Button Content="Nouveau" Click="New_Click" Margin="0,0,8,0"/>
                    <Button Content="Enregistrer" Click="Save_Click" Margin="0,0,8,0"/>
                    <Button Content="Supprimer" Click="Delete_Click" Margin="0,0,8,0"/>
                    <Button Content="Recharger" Click="Reload_Click"/>
                </StackPanel>

                <!-- Formulaire -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Nom" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Nom, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Prenom" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Prenom, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Nom Team" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.NomTeam, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Societe" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Societe, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="SIRET" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Siret, UpdateSourceTrigger=PropertyChanged}" Grid.Row="4" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Adresse" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Adresse, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Code postal" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.CodePostal, UpdateSourceTrigger=PropertyChanged}" Grid.Row="6" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Ville" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Ville, UpdateSourceTrigger=PropertyChanged}" Grid.Row="7" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Email" Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Email, UpdateSourceTrigger=PropertyChanged}" Grid.Row="8" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Telephone" Grid.Row="9" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Telephone, UpdateSourceTrigger=PropertyChanged}" Grid.Row="9" Grid.Column="1" Margin="0,0,0,8"/>

                    <TextBlock Text="Notes" Grid.Row="10" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,8,8"/>
                    <TextBox Text="{Binding SelectedClient.Notes, UpdateSourceTrigger=PropertyChanged}" Grid.Row="10" Grid.Column="1" Margin="0,0,0,8" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto"/>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
