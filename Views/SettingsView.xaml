<!-- Views/SettingsView.xaml -->
<UserControl x:Class="VorTech.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12" Background="{StaticResource Panel}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- 1) Profil fiscal -->
    <GroupBox Header="Profil fiscal (Micro)">
      <StackPanel Orientation="Horizontal" Margin="10,8,10,12">
        <!-- NOM EXACT attendu par le .cs : Chk293B -->
        <CheckBox x:Name="Chk293B"
                  Content="Micro-entrepreneur — TVA non applicable (art. 293 B du CGI)"/>
      </StackPanel>
    </GroupBox>

    <!-- 2) Moyens de règlement -->
    <GroupBox Grid.Row="1" Header="Moyens de règlement" Margin="0,8,0,8">
      <Grid Margin="10">
        <Grid.RowDefinitions>
          <RowDefinition Height="*"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- NOM EXACT attendu par le .cs : GridPay -->
        <DataGrid x:Name="GridPay"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  Margin="0,0,0,8">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="2*"/>
            <DataGridTextColumn Header="Frais fixes (€)" Binding="{Binding FixedFee}" Width="*"/>
            <DataGridTextColumn Header="Commission (%)" Binding="{Binding PercentFee}" Width="*"/>
          </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
          <Button Content="Enregistrer"/>
        </StackPanel>
      </Grid>
    </GroupBox>

    <!-- 3) Identité & Banque -->
    <GroupBox Grid.Row="2" Header="Identité &amp; Banque">
      <Grid Margin="10">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="200"/>
          <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="SIREN / SIRET"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Siret}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="IBAN"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Iban}"/>

        <Label Grid.Row="2" Grid.Column="0" Content="BIC"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Bic}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Nom commercial"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding BusinessName}"/>
      </Grid>
    </GroupBox>
  </Grid>
</UserControl>
