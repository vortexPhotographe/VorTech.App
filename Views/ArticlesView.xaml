<UserControl x:Class="VorTech.App.Views.ArticlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="8"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Barre d'actions -->
    <StackPanel Orientation="Horizontal" Grid.Row="0">
      <Button Content="Nouveau"    Click="New_Click"/>
      <Button Content="Enregistrer" Click="Save_Click"   Margin="8,0,0,0"/>
      <Button Content="Supprimer"   Click="Delete_Click" Margin="8,0,0,0"/>
      <Button Content="Recharger"   Click="Reload_Click" Margin="8,0,0,0"/>
    </StackPanel>

    <Border Grid.Row="1" Height="8" Background="Transparent"/>

    <!-- Corps -->
    <Grid Grid.Row="2">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="12"/>
        <ColumnDefinition Width="2*"/>
      </Grid.ColumnDefinitions>

      <!-- Liste -->
      <GroupBox Grid.Column="0" Header="Articles">
        <DataGrid x:Name="GridArticles"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="GridArticles_SelectionChanged">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Id"      Binding="{Binding Id}"      Width="60"/>
            <DataGridTextColumn Header="SKU"     Binding="{Binding Sku}"     Width="*"/>
            <DataGridTextColumn Header="Nom"     Binding="{Binding Name}"    Width="2*"/>
            <DataGridTextColumn Header="PU HT"   Binding="{Binding PriceHT}" Width="*" />
            <DataGridTextColumn Header="Stock"   Binding="{Binding Stock}"   Width="*" />
          </DataGrid.Columns>
        </DataGrid>
      </GroupBox>

      <Border Grid.Column="1" Width="12" Background="Transparent"/>

      <!-- Formulaire -->
      <GroupBox Grid.Column="2" Header="Fiche article">
        <Grid Margin="8">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Label Grid.Row="0" Grid.Column="0" Content="SKU"/>
          <TextBox x:Name="SkuBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

          <Label Grid.Row="1" Grid.Column="0" Content="Nom"/>
          <TextBox x:Name="NameBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

          <Label Grid.Row="2" Grid.Column="0" Content="PU HT"/>
          <TextBox x:Name="PriceBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

          <Label Grid.Row="3" Grid.Column="0" Content="Stock"/>
          <TextBox x:Name="StockBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>
        </Grid>
      </GroupBox>
    </Grid>
  </Grid>
</UserControl>
