<UserControl x:Class="VorTech.App.Views.ArticlesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="12">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="320"/>
      <ColumnDefinition Width="12"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <!-- Liste -->
    <GroupBox Header="Articles" Grid.Column="0">
      <ListBox x:Name="List"
               DisplayMemberPath="Libelle"
               SelectionChanged="List_SelectionChanged"/>
    </GroupBox>

    <Border Grid.Column="1"/>

    <!-- Fiche -->
    <GroupBox Header="Fiche" Grid.Column="2">
      <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8" Orientation="Vertical">
          <TextBlock Text="Référence"/>
          <TextBox x:Name="TbRef" Margin="0,0,0,6"/>

          <TextBlock Text="Libellé"/>
          <TextBox x:Name="TbLib" Margin="0,0,0,6"/>

          <TextBlock Text="Prix achat HT"/>
          <TextBox x:Name="TbPA" Margin="0,0,0,6"/>

          <TextBlock Text="Prix vente HT"/>
          <TextBox x:Name="TbPV" Margin="0,0,0,6"/>

          <TextBlock Text="Stock actuel"/>
          <TextBox x:Name="TbStock" Margin="0,0,0,6"/>

          <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Nouveau" Click="New_Click"/>
            <Button Content="Enregistrer" Margin="8,0,0,0" Click="Save_Click"/>
            <Button Content="Supprimer"   Margin="8,0,0,0" Click="Delete_Click"/>
          </StackPanel>
        </StackPanel>
      </ScrollViewer>
    </GroupBox>
  </Grid>
</UserControl>
