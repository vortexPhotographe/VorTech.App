<Window x:Class="VorTech.App.Dialogs.ReassortDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Réassort" Width="420" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource Bg}" Foreground="{StaticResource Fore}">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="140"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>

    <Label Grid.Row="0" Grid.Column="0" Content="Fournisseur"/>
    <TextBox x:Name="TxtFournisseur" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

    <Label Grid.Row="1" Grid.Column="0" Content="Quantité"/>
    <TextBox x:Name="TxtQte" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

    <Label Grid.Row="2" Grid.Column="0" Content="PU Achat HT"/>
    <TextBox x:Name="TxtPU" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Annuler" IsCancel="True"/>
      <Button Content="Valider" Margin="8,0,0,0" IsDefault="True" Click="Validate_Click"/>
    </StackPanel>
  </Grid>
</Window>
